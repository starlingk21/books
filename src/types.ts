// This only exists so types.ts is always recognized as a module
// Add your custom types and remove this Dummy type when you're done
export interface Book {
  title?: string;
  author?: string;
  isbn?: string;
  cover?: string;
}
